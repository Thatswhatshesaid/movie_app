<%= form_for(@movie_review) do |f| %>
  <% if @movie_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie_review.errors.count, "error") %> prohibited this movie_review from being saved:</h2>

      <ul>
      <% @movie_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_reviews %><br />
    <%= f.text_field :movie_reviews %>
  </div>
  <div class="field">
    <%= f.label :movies_id %><br />
    <%= f.number_field :movies_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
